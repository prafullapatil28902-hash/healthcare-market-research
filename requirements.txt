# Healthcare Market Research Report Generator — Python dependencies
# Tested with Python 3.10–3.12

streamlit>=1.32
pandas>=2.0
plotly>=5.18
openpyxl>=3.1
reportlab>=4.0

# SQLite ships with the Python standard library (sqlite3) — no extra package
# needed.
